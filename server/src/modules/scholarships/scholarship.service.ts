export class ScholarshipService {
  static async getScholarshipById(id: number) {
    // Placeholder: fetch scholarship by id
    return { id, name: "Sample Scholarship" };
  }

  static async listScholarships() {
    // Placeholder: fetch all scholarships
    return [];
  }
}
