export class ApplicationService {
  static async getApplicationById(id: number) {
    // Placeholder: fetch application by id
    return { id, name: "Sample Application" };
  }

  static async listApplications() {
    // Placeholder: fetch all applications
    return [];
  }
}
