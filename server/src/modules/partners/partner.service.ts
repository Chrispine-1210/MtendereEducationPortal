export class PartnerService {
  static async getPartnerById(id: number) {
    // Placeholder: fetch partner by id
    return { id, name: "Sample Partner" };
  }

  static async listPartners() {
    // Placeholder: fetch all partners
    return [];
  }
}
