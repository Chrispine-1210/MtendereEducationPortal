export class TestimonialService {
  static async getTestimonialById(id: number) {
    // Placeholder: fetch testimonial by id
    return { id, content: "Sample Testimonial" };
  }

  static async listTestimonials() {
    // Placeholder: fetch all testimonials
    return [];
  }
}
