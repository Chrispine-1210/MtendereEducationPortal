export class CourseService {
  static async getCourseById(id: number) {
    // Placeholder: fetch course by id
    return { id, title: "Sample Course" };
  }

  static async listCourses() {
    // Placeholder: fetch all courses
    return [];
  }
}
