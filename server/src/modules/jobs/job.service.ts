export class JobService {
  static async getJobById(id: number) {
    // Placeholder: fetch job by id
    return { id, title: "Sample Job" };
  }

  static async listJobs() {
    // Placeholder: fetch all jobs
    return [];
  }
}
